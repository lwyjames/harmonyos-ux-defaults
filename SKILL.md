---
name: apply-harmonyos-ux-defaults
description: Apply the user's global HarmonyOS UX baseline, wide-foldable/Pura X Max rules, mapped phone bezels, security/privacy branding, and Xiaoyi AI assistant patterns to UX requirements, flows, screens, wireframes, mockups, image prompts, clickable/frontend prototypes, service cards, notifications, Live View, Picture-in-Picture, Flash Control Ball/Window, sharing, messaging, WeChat, multi-device experiences, and handoffs. Invoke automatically when a UX task lacks another platform specification, and whenever HarmonyOS/Huawei devices, Xiaoyi/小艺, conversational AI, security/privacy, anti-fraud, secure preview, permission recall, phone OS UX, system surfaces, video continuity, cross-app controls, mobile apps, wide foldables, compact outer displays, Pura X-like ratios, foldables, tablets, PCs, watches, cockpit displays, or cross-device continuity are involved. Also use when reviewing designs against the user's default UX standard.
---

# Apply HarmonyOS UX Defaults

Use the supplied HarmonyOS developer design documentation as the user's default UX baseline. Treat it as a working specification, not visual inspiration.

## Resolve precedence

Apply rules in this order:

1. Follow the user's explicit instruction in the current request.
2. Preserve approved project-specific decisions, source-of-truth manifests, supplied screenshots, and named target-platform conventions.
3. Apply this HarmonyOS baseline.
4. Use general UX judgment only where the baseline is silent.

Do not let a generic rule overwrite a locked project decision. If the user explicitly targets iOS, Material Design, Windows, web, automotive, or another design system, use that system for platform-specific chrome and components; retain compatible cross-platform principles such as responsiveness, clear hierarchy, native interaction, accessibility, and state continuity. If the platform is unspecified, default to HarmonyOS.

## Start every task

1. Identify the target device, window state, orientation, system surface, input modes, and expected deliverable.
2. Read [quick-reference.md](references/quick-reference.md) in full.
3. Whenever the deliverable renders text, read [harmonyos-sans-font-assets.md](references/harmonyos-sans-font-assets.md). Resolve the locale-appropriate external font with `scripts/resolve_harmonyos_fonts.py` before final high-fidelity rendering. On Windows, prefer the configured external font directory. In ChatGPT Work/cloud, use the exact account asset `HarmonyOS+Sans.zip` when available, materialize and extract it only into temporary task storage, and checksum-verify it. Do not create a font-only dependent skill and do not silently substitute a different font.
4. For every screen or prototype that includes system chrome, read [system-chrome-verification.md](references/system-chrome-verification.md). When the navigation bar is present, also read [navigation-bar-geometry.md](references/navigation-bar-geometry.md). Resolve status-bar and navigation-bar behavior per state before positioning app-owned content.
5. For phone mockups, prototypes, or rendered screens, read [device-preview-frames.md](references/device-preview-frames.md) and use the mapped bezel asset. When a hand or finger holds, touches, approaches, or occludes the phone, also read [phone-in-hand-contact-compositing.md](references/phone-in-hand-contact-compositing.md), build a per-boundary contact map, and follow its device-first depth, masking, matting, ergonomic, and regression workflow for **every phone model and both portrait and landscape orientations**. For people, furniture, or another physical scene without hand contact, follow the device-first scene-compositing workflow in `device-preview-frames.md`.
6. Whenever a phone home screen, launcher, desktop, status bar, or navigation bar appears, read [home-screen-defaults.md](references/home-screen-defaults.md) and apply its exact mapped assets and raster-chrome requirements. Whenever a phone lock screen, lock-screen clock, or lower lock-screen shortcut appears, also read [lock-screen-visual-defaults.md](references/lock-screen-visual-defaults.md). When an ordinary notification is rendered as an expanded/detail lock-screen card, additionally read [lock-screen-notification-card.md](references/lock-screen-notification-card.md); do not confuse it with the transient top Push banner.
7. Whenever a request explicitly shows WeChat, asks for a WeChat conversation, or uses a WeChat chat as the background beneath a system surface, read [wechat-chat-interface.md](references/wechat-chat-interface.md). Apply it only to the app-owned chat layer; ignore the reference screenshot's status bar and keep HarmonyOS system chrome authoritative. Never bundle or reproduce the source screenshot or its private content.
8. Whenever a request shows or asks for the HarmonyOS system AI assistant Xiaoyi/小艺 full-screen conversation interface, read [xiaoyi-assistant-interface.md](references/xiaoyi-assistant-interface.md). Treat the Pura X View captures only as the component-relationship reference, derive every other phone/window responsively from its logical canvas and device class, ignore the captures' complete status bars, and never bundle or reproduce their private content.
9. For a Pura X/Pura X Max-like wide foldable, a similarly proportioned compact screen, an outer-screen experience, or a fold/unfold transition, read [wide-foldable-pura-x-max.md](references/wide-foldable-pura-x-max.md). Apply its outer-screen rules only to the outer/compact display state, not automatically to Pura X Max unfolded portrait.
10. Use [source-index.md](references/source-index.md) to select every relevant extracted source text. Read the selected text completely before making detailed design decisions.
11. For any transient phone Push Notification banner, read [push-notification-banner.md](references/push-notification-banner.md). Apply its screenshot-derived responsive portrait geometry and material to every app's push banner without turning the example's WeChat identity, red wallpaper, or message into a universal asset or color.
12. For any 实况窗/Live View card, capsule, expanded capsule, or compact circular outer-display treatment, read [live-view-sketch-spec.md](references/live-view-sketch-spec.md). For every expanded phone Live View card, also read [expanded-live-view-card.md](references/expanded-live-view-card.md); its responsive geometry, placement, radius, material, and internal-region rules supersede conflicting expanded-card values from the older Sketch template without changing other Live View forms or behavior.
13. Open or render the matching source PDF when diagrams, component anatomy, examples, crop behavior, safe areas, or visual relationships affect the answer. The PDF controls over extracted text when extraction loses layout.
14. State any consequential assumption. Do not invent a HarmonyOS component, token, behavior, or numerical value when the sources are silent.
15. Before emitting an exact value that is not in the quick reference, locate it in the selected source text, the Live View Sketch reference, the push-banner supplement, the Xiaoyi supplement, or the relevant PDF figure. If it cannot be verified, omit it or label it as a project recommendation rather than a HarmonyOS requirement.
16. For every image-generation or raster-rendering request, resolve the delivery mode before rendering. Unless the user explicitly requests native/original resolution, a high-resolution final, print/final-delivery quality, or exact pixel dimensions, use the lightweight-preview rules below.

## Global defaults

- Default unspecified mobile OS work to HarmonyOS NEXT visual and interaction language.
- Use Simplified Chinese for user-facing screen copy unless the user requests another language. Write specifications and handoffs in the language requested by the user.
- Use the checksum-verified external `HarmonyOS_Sans_SC.ttf` as the default font binary for Simplified Chinese and mixed Chinese/Latin HarmonyOS UI. Select the locale-appropriate supplied HarmonyOS Sans file for other languages and treatments. Keep all font binaries outside this skill, never modify or subset them, and never silently substitute another font in a final high-fidelity output. Follow [harmonyos-sans-font-assets.md](references/harmonyos-sans-font-assets.md).
- For every unspecified UX design requirement that needs a phone, default to Pura X Max unfolded portrait at 1828 x 2584 px and 665 x 940 vp. Use `assets/device-bezels/pura-x-max-unfolded-portrait.png` as its bezel. A current-request device, state, orientation, or bezel instruction overrides this default.
- For Pura X Max unfolded landscape, use 2584 x 1828 px and 940 x 665 vp. Rotate the official portrait bezel **90 degrees clockwise** before compositing so its front-facing camera is at the upper-right corner. Rotate only the untouched bezel source; render the UI separately in landscape and re-run alpha-derived aperture compositing. Never rotate a finished portrait composite.
- For Pura X View landscape, use 2232 x 1320 px and 744 x 440 vp. Rotate the untouched `assets/device-bezels/pura-x-view-front.png` source exactly **90 degrees counterclockwise** so the front-facing camera is on the **left side** of the landscape phone, render the UI independently in landscape, and re-run alpha-derived aperture compositing. Never ask an image-generation model to redraw the Pura X View as a generic landscape phone.
- For Pura X View front portrait, the corrected mapped bezel carries the lock-screen-reference camera module: one centered circular optical opening with center `(220,27) vp` and outer diameter `22 vp`. It has a black concentric rim, dark glass lens, and restrained blue optical highlight, with no square tile, colored backing, notch, Dynamic Island, or duplicate dot. This Pura X View-specific camera geometry rotates with the mapped bezel in landscape.
- Treat Pura X Max unfolded portrait as an inner/unfolded state. Do not hide its status/navigation bars or remove its multi-window support solely because the new wide-foldable supplement contains outer-screen rules. Resolve the screen role, fold state, and current window dimensions first.
- Include the corresponding designated phone bezel around every phone screen in every visual UX output by default. This applies to wireframes, high-fidelity mockups, screen-flow panels, keyframes, generated images, presentation visuals, and prototype previews. Omit the bezel only when the user explicitly requests a bezel-free, screen-only, raw-canvas, or implementation-only asset.
- Visibly render the HarmonyOS system navigation bar inside every normal full-screen phone app frame by default, including the default Pura X Max unfolded portrait frame. Do not omit it for visual cleanliness, extra content space, or because a hardware bezel is present. Omit it only when the user explicitly requests otherwise or the documented state is a floating window, Picture-in-Picture, Pura X/Pura X Max-like compact outer display, or the hidden phase of a qualifying immersive experience. For a **single static game mockup** whose phase is not specified, deliberately represent the post-timeout hidden phase and omit the indicator; this static-review default does not change the game's runtime navigation lifecycle.
- In every phone canvas where the navigation bar is present, render the visible indicator as a horizontally centered **6 vp-high capsule** whose bottom edge is **6 vp above the visible bottom of the screen opening**. Let `W` be the current logical screen width: use width `112 vp` for `360 <= W < 600`, `W / 3 - 48 vp` for `600 <= W <= 840`, and `W / 4 - 48 vp` for `W > 840`. Derive the visible screen bottom from the mapped bezel's center-connected alpha aperture in a bezeled raster output. Keep the separate invisible, bottom-anchored interaction region at `35%` of `W` by `28 vp`; app-owned bottom content must clear this region. Do not reuse the superseded fixed `1252 x 84 px`, 42 px radius, or 16 px offset.
- For ordinary light app surfaces, continue the app's bottom surface behind the navigation region and render the indicator with the screenshot-derived project default `rgba(0, 0, 0, 0.08)`. On a surface near `#F6F6F6`, the expected flattened result is near `#E3E3E3`. Treat this as a neutral translucent system-chrome convention, not an official recovered token or an opaque fixed gray. For dark, colored, image, or high-variance backgrounds, use system semantic inversion/adaptation and verify visibility rather than reusing 8% black. Follow [navigation-bar-geometry.md](references/navigation-bar-geometry.md).
- Default static phone time to **08:08** unless the user or scenario requires another time. At the status bar's right end, follow `assets/system-chrome/status-bar-right-default-reference.png`: left-to-right NearLink, Wi-Fi 7, dual stacked 5A signals, and battery 100.
- For the Pura X Max unfolded portrait, Pura X View front, and Pura 90 Pro Max portrait default home screens, use their exact mapped PNGs in `assets/default-homescreens/`. For other phones, preserve the masters' icon/widget design and layout language while adapting to the documented canvas.
- Treat pixel dimensions as mockup canvases, never as fixed implementation geometry. Annotate logical layout in vp/fp.
- Treat the mapped official bezel PNG as the sole source of truth for screen-opening geometry, corner curves, antialiasing, camera position, frame/hinge details, and transparency. Never approximate its aperture with a manually drawn rounded rectangle, guessed radius, generic device mask, or hard-coded geometry unless the bezel package explicitly supplies those exact values.
- Render the app UI as a separate, fully opaque screen layer at the documented screen resolution and aspect ratio. Preserve its visible screen coordinates: never shift, stretch, or rescale the visible UI merely to cover a bezel seam.
- Place the screen layer beneath the official bezel and create hidden overscan by extruding its first/last rows, first/last columns, and four corner pixels beneath the upper, bottom, left, right, and diagonal corner lips. Derive each side's overscan independently from that bezel's alpha transition band plus a small hidden safety margin; never apply one universal bleed value to every asset.
- Derive the interior aperture and exterior transparent component programmatically from the official bezel alpha channel. Every partially or fully transparent inner-aperture pixel must have fully opaque screen content beneath it, while no screen content may enter the exterior transparent component. Never substitute a generic rounded-rectangle mask.
- Composite the unmodified official bezel as the final top layer. A native final uses its native pixel dimensions and is never resized after compositing; a lightweight preview uses only the single uniform preview scale defined below. Do not stretch, crop, recolor, redraw, or generatively recreate it. The only standard orientation transforms are the mapped source-bezel rotations: Pura X Max unfolded landscape rotates exactly 90 degrees clockwise, while Pura X View front landscape rotates exactly 90 degrees counterclockwise.
- The device-first scene-compositing rule is universal across all current and future mapped phone models, fold states, form factors, and portrait/landscape holding scenes. Pura X View is only one mapped example and never limits the rule's scope. Resolve the selected model's orientation-specific screen canvas and official bezel before scene work; if that orientation has no verified mapping or approved bezel transform, request or establish the correct mapping instead of substituting a generic phone.
- When hands, a person, or an environmental scene is required, freeze the validated screen-plus-bezel result as a single device master before creating or compositing those elements. Generate or source hands/backgrounds separately and place them behind or in front of the frozen device master with explicit masks. Never generate “phone + hands” in one pass, let the scene generator infer the handset silhouette, or warp, regenerate, redraw, resize, perspective-transform, or round the device master to fit a hand pose. Translation on the larger scene canvas is allowed; geometric deformation is not. If the hand and device do not fit in either orientation, adjust or regenerate the hand/scene layer—not the phone.
- Scene layers may naturally occlude portions of the already-correct device, but they must not alter any unoccluded device pixel, screen aspect ratio, bezel contour, corner curve, cutout/camera position, hinge, navigation geometry, or UI coordinate. Reuse the same frozen device master when producing otherwise identical variants such as “without notification” and “with notification.”
- For every phone-hand boundary, classify each segment as `foreground_occlusion`, `behind_device`, `physical_contact`, or `natural_clearance` before generating or masking. A declared contact may not contain a background-colored seam; a rear finger may not cover the bezel; a foreground thumb/finger mask must contain the full visible skin silhouette so the frame cannot pass through it. Preserve intentional anatomical clearance. Follow [phone-in-hand-contact-compositing.md](references/phone-in-hand-contact-compositing.md).
- When no mapped or supplied bezel exists, depict the front-facing camera as one simple round dot rather than a notch or Dynamic Island.
- Prefer HarmonyOS system components and native system flows, including Picture-in-Picture and Flash Control Ball/Window when the use case qualifies, over custom imitations.
- Use system semantic tokens rather than isolated raw color values. Define both light and dark behavior.
- Use responsive, mobile-first layout. Do not stretch one fixed phone composition across other windows.
- Preserve safe areas, status/navigation behavior, hinge avoidance, keyboard space, large text, and multi-window continuity.
- Keep the user's primary task unobstructed. Decorative motion, immersive materials, AI, cards, notifications, and system surfaces remain subordinate to utility and system priority.
- For a transient Push Notification on any designated portrait phone, use the responsive banner frame `(16,50,W-32,64) vp` with `16 vp` radius, where `W` is the visible logical canvas width. Preserve the complete sharp underlay, then overlay the banner without reflowing it. Use the actual local underlay for clipped color-preserving blur, a screenshot-reconstructed `32%` neutral-dark overlay, a subtle `24%` neutral-light outline, and independent full-strength icon/text content; never reuse the example's red hue. Follow [push-notification-banner.md](references/push-notification-banner.md).
- For an ordinary expanded/detail notification on a portrait lock screen, use the separate responsive card model in [lock-screen-notification-card.md](references/lock-screen-notification-card.md): `x=max(16,safeInsets)`, width `W-2x`, height `64 vp`, radius `16 vp`, and bottom edge `27 vp` above the resolved lower-shortcut top. On Pura X View this resolves to `(16,554,408,64) vp`. Preserve the frozen lock screen outside the rounded mask, use locally wallpaper-derived thin glass with the reconstructed `28%` tint and `40%` outline defaults, keep logo/text independently opaque, omit the transient-banner bottom handle, and honor the system lock-screen preview setting. Never copy the sample's app, sender, message, or red hue.
- For the Xiaoyi/小艺 full-screen phone assistant, use a pinned glass app bar, one scrolling mixed-content conversation stream, and a pinned glass capability/composer stack. The screenshot-defined glass construction, top-right sparkling-call/muted-speaker/four-small-dot controls, four fixed icon-label chips, trailing three-sparkle action, AI-notice spacing, and asymmetric blue user-message shape are normative rather than examples. A single-line user message is `44 vp` high with corner radii `(22,0,22,22) vp` in top-left/top-right/bottom-right/bottom-left order; its upper-right corner is square. The sparkling telephone uses a smaller upper star and larger lower star. Do not add a standalone bottom-right down-arrow or scroll-to-latest control. Resolve device edge margin `E`, conversation inset `T=E+8 vp`, and active column `K`; compute the composer as `(E, *, K-2E, 48) vp`. Never globally scale the Pura X View reference or copy its status bar. Follow [xiaoyi-assistant-interface.md](references/xiaoyi-assistant-interface.md).

## Image delivery mode

Default every image-generation, raster mockup, UX keyframe, illustration, and presentation-image task to **lightweight preview**. Do not render, export, save, or deliver a native-resolution image unless the user explicitly requests native/original resolution, a high-resolution final, print/final-delivery quality, or exact pixel dimensions. A request for “mockup,” “high fidelity,” “polished,” or “PNG” alone does not imply native resolution.

For lightweight preview:

- Preserve the approved aspect ratio, complete composition, logical vp/fp geometry, device identity, bezel silhouette, camera/cutout side, UI hierarchy, text, and relative coordinates. It must be the same design at a smaller review resolution, not a simplified, cropped, reflowed, or differently composed version.
- Use the image tool's standard/lightweight preview setting. When a pixel target must be chosen, keep the longest output edge at or below **1600 px** while preserving the exact aspect ratio as closely as integer pixels allow.
- Treat documented native pixels as the coordinate and geometry source only. Calculate the preview scale once and apply that one uniform scale to the complete screen, official bezel, overlays, hands, people, and scene. Never scale layers independently or ask a generator to recreate the device at the preview size.
- For mapped-device compositing, it is permissible to create a uniformly downscaled working copy of the official bezel solely for the preview. Preserve the original asset untouched, derive the preview aperture from the downscaled alpha channel, and run preview-scale edge/corner checks. Defer native-pixel seam validation and the native output file until the user requests the native version.
- Do not create an additional native-size PNG “just in case.” Retain logical measurements and source-layer instructions so an approved preview can later be rendered natively without redesign.
- Name or identify the result as a preview when filenames or captions are present.

The rule that a completed mapped-device composite must not be resized refers to a native final master. Lightweight preview is the sole delivery exception: use one proportional whole-composition scale, with no independent layer resizing, geometry changes, crop, reflow, or generative reconstruction.

When delivering a lightweight preview, end the user-facing response with this sentence in Chinese, or a faithful localized equivalent when the task language is not Chinese:

> 这是轻量预览版；如果您确认效果满意，我可以继续生成原生尺寸版本。

Use the sentence once after all preview images. Do not include it when the user explicitly requested and received native/original-size output.

## Official security & privacy branding

For any HarmonyOS security- or privacy-related UX, prototype, mockup, keyframe, system alert, notification, launch visual, or developer handoff, read and follow [security-privacy-branding.md](references/security-privacy-branding.md).

When a branded security/privacy emblem or shield visual is required, use the official user-provided 鸿蒙星盾安全 standard-color shield asset from `assets/branding/`. Do not invent, redraw, approximate, or generatively recreate a substitute shield. Preserve the official geometry, proportions, transparency, and standard colors. Prefer placing the supplied asset directly into the composition. Use the transparent asset for raster workflows; when a dark background would reduce contrast, change the surrounding container or surface rather than recoloring the logo unless an official alternate colorway has been supplied.

## Design workflow

### 1. Select the system pattern

Determine whether the request belongs in an app page, system share sheet, notification, Live View, service card, Picture-in-Picture, Flash Control Ball/Window, multi-window mode, Tap-to-Share flow, or another system surface. Use the native pattern and its stated limits. Do not substitute a visually similar custom surface.

When Live View is selected, use the documented forms instead of a generic notification or guessed pill: `336 x 72 vp` base/collapsed card, responsive expanded phone card, `80 x 28 vp` default status-bar capsule, or `128 x 28 vp` expanded capsule when verified status-bar space permits. For an expanded phone card, first build a complete normal card-free app state, freeze every app-owned coordinate and scroll position, then composite the card above it; never use `cardY`, `cardHeight`, `cardBottom`, or a card-clearance spacer to lay out the app. On logical canvas width `W`, use frame `(16, 50, W - 32, (W - 32) x 6 / 17) vp` and radius `20 vp`. Compose its material bottom-to-top from the actual frozen underlay, offscreen blur that preserves broad hue/luminance and is clipped by the final rounded mask after filtering, neutral-dark overlay at 48% opacity, thin neutral outline at 25% opacity, and full-strength content inside a 100%-opacity root group. Never blur the app root, card parent, pre-masked alpha, or completed composite; never flatten the card to uniform gray, add exterior haze/glow/broad shadow, bake a decorative tint, or change the 48% overlay. Outside the one-pixel antialias boundary allowance, the card-present app must remain pixel-identical to the sharp card-free base. Preserve the HarmonyOS status bar and perceptually occlude covered app detail; do not manufacture an empty band below the card or move normally preceding content to `cardBottom + gap`. Treat the capsule as system-owned: center it with a centered punch hole, place it left on side-punch/no-hole devices, and preserve collision behavior with fixed status content. Read [live-view-sketch-spec.md](references/live-view-sketch-spec.md) and [expanded-live-view-card.md](references/expanded-live-view-card.md) for exact scope, local regions, material, circular outer-display treatment, and verification.

When a push is shown as a transient portrait banner, use the native notification structure and [push-notification-banner.md](references/push-notification-banner.md), not a generic toast, Live View card, or custom advertising surface. The screenshot-derived Pura X View result is `(16,50,408,64) vp`; on every other designated portrait phone recalculate the width as `W-32` while keeping `x=16`, `y=50`, height `64`, and radius `16 vp`. Use the system-resolved app/service icon, a contextual one-line title, a one-line detail, a trailing time, and the centered bottom affordance. The affordance must inherit a lightened, low-chroma tint from the completed local banner material and be alpha-composited translucently; never flatten it as fixed pure white. Build and freeze the normal app/home underlay first, then composite the banner above it; the underlay must not move, blur, dim, or recolor outside the rounded banner. The red wallpaper and WeChat content are example-only and never become assets or universal styling.

### 2. Define responsive structure

Specify the logical vp/fp canvas, safe-area treatment, grid, margins, gutters, breakpoints, and layout transformation. Cover the states that materially affect the experience: compact/medium/expanded width, portrait/landscape, fold/unfold or hinge, split/floating window, keyboard, and large text.

### 3. Resolve system chrome

Before placing app-owned top or bottom controls, decide the status bar and navigation bar for every relevant state. Record whether each bar is visible, temporarily hidden, absent, or system-owned; define the content/background extension, status-content color, cutout avoidance, bottom interaction region, scroll-end behavior, modal layering, and reveal gesture. If no qualifying exception is identified, set the navigation bar to **visible** and calculate its responsive logical geometry from [navigation-bar-geometry.md](references/navigation-bar-geometry.md): 6 vp visible height, 6 vp bottom-edge gap, centered responsive width, and a separate bottom-anchored `35% W x 28 vp` invisible interaction region. Default static time to 08:08 and render the reference NearLink/Wi-Fi 7/dual-5A/battery-100 cluster at the right. For a single static **game** mockup with no phase specified, select the post-timeout hidden state and omit the navigation indicator. For other single static immersive mockups with no phase specified, show the initial/default visible state. For a multi-frame immersive sequence, show both the initial/revealed state and the post-timeout hidden state when relevant. Use [system-chrome-verification.md](references/system-chrome-verification.md) as the required state matrix and acceptance checklist. Do not apply a single fixed inset to normal, split, floating, Picture-in-Picture, immersive, landscape, keyboard, and compact outer-display states.

### 4. Apply visual tokens

Use the HarmonyOS typography roles, semantic colors, spacing levels, and hierarchical corner radii. Resolve the locale-appropriate checksum-verified external HarmonyOS Sans file before rendering text, and use its exact path rather than an unverified family lookup. Use Immersive Light only where an overlaying control needs separation from content and select its material level by placement.

### 5. Define interaction and system states

Show default, pressed, selected, disabled, loading, empty, offline, error, permission-denied, interruption, and recovery states when relevant. Preserve task state across rotation, resizing, folding, window-mode changes, exit, restart, and notification dismissal when the flow requires continuity.

### 6. Write UI copy

Use concise, respectful, action-oriented Chinese. Lead with user value or the actionable result. Use native terminology and the source punctuation/spacing rules. Avoid technical jargon, inflated marketing language, repeated context, and unnecessary exclamation marks.

### 7. Composite the device bezel

For every rendered phone, retain separate app-screen and official-bezel source layers. Render the screen fully opaque at the documented resolution and aspect ratio. Preserve its visible coordinates, then create hidden overscan by extruding the first/last rows and columns plus the four corner pixels beneath all four bezel lips and corners. Calculate top, bottom, left, and right overscan independently from the mapped bezel's alpha channel. Separate the center-connected opening from exterior transparency, clip the underlay against the exterior component, and composite the untouched bezel last at native size.

For every phone-in-hand or phone-in-scene visual—regardless of model, fold state, form factor, or portrait/landscape orientation—use this fixed layer order:

1. Render the complete screen-only UX at the mapped orientation-specific raster size.
2. Composite and validate the mapped official bezel to create a frozen device master.
3. Create the background/person/hands independently; never include a substitute phone in that generation.
4. Composite the frozen device master into the scene, then place only the intended hand/finger occlusions above it with explicit masks.
5. Compare all unoccluded device pixels and landmarks against the frozen master. Any changed geometry or unmasked pixel rejects the scene.

For phone-in-hand work, split hand regions by the approved contact map rather than treating the whole hand as one front or rear layer. Preserve the fixed background → rear hand → frozen device → foreground hand order, and run the contact, matting, bilateral, and local-regression checks in [phone-in-hand-contact-compositing.md](references/phone-in-hand-contact-compositing.md).

Use `scripts/composite_validate_bezel.py` for deterministic raster compositing and validation. Do not substitute a manual rounded rectangle or generative repair. Read [device-preview-frames.md](references/device-preview-frames.md) for the required command, diagnostics, and interpretation.

### 8. Verify before delivery

Check:

- platform pattern and system surface are correct;
- every phone screen in every visual output is enclosed by the corresponding designated bezel unless the user explicitly requested a bezel-free or screen-only deliverable;
- the mapped official bezel remains pixel-identical in dimensions and proportions, and no manually estimated rounded-rectangle or generic aperture mask controls the screen opening;
- the app-screen source is fully opaque at the documented resolution/aspect ratio, its visible coordinates remain unchanged, and its edge pixels are extruded into independently calculated upper, bottom, left, right, and diagonal hidden overscan;
- every rendered phone passes bezel-seam inspection at 400% zoom or an equivalent pixel-level check across the complete upper, bottom, left, and right inner edges, all four corners, and every straight-edge/corner transition on white, black, and a saturated diagnostic background;
- every visual containing hands, people, or a physical environment was assembled from a separately completed and validated device master; there was no one-pass generation of the handset and scene;
- the device-first rule was applied without a model or orientation exception: portrait and landscape holding scenes for Pura X View, Pura X Max, Pura 90 Pro Max, other mapped phones, and future mapped phones all follow the same separation and validation workflow;
- outside an explicit hand/foreground occlusion mask, the scene's device pixels, outer silhouette, aperture, cutout/camera, hinge, UI scale, and screen coordinates match the frozen device master exactly; a bad hand fit is repaired only in the hand/scene layer;
- every phone-in-hand boundary has an explicit contact-map classification; physical-contact segments have no background seam or matte halo, behind-device fingers do not cover the frame, foreground hand masks prevent bezel-through-skin, intentional anatomical clearance remains visible, fingertips/nails are complete, and corresponding left/right grip relationships are coherent;
- every local hand/contact correction preserves the last approved composite outside its declared edit region and rechecks all previously approved thumb, index, finger-root, side-rail, and corner relationships so a fix cannot regress another region;
- automated validation reports zero uncovered pixels separately for the upper edge, bottom edge, left edge, right edge, upper-left corner, upper-right corner, bottom-left corner, and bottom-right corner; every inner-aperture pixel with official-bezel alpha below full opacity has fully opaque screen content beneath it;
- the final exterior transparency matches the official bezel silhouette, with zero screen pixels in the exterior transparent component;
- there is no white or background-colored seam, transparent gap, one-pixel hairline, exposed mask edge, incorrect corner curvature, missing-underlay halo, straight-edge/corner discontinuity, or screen-content leakage outside the device silhouette;
- **Acceptance criterion:** “The active screen continuously meets the official bezel’s antialiased inner boundary across the complete upper, bottom, left, and right edges and all four corners. Every partially or fully transparent inner-aperture pixel has opaque screen content beneath it, every directional uncovered-pixel count is zero, and no screen content leaks outside the official device silhouette.”
- any bezel-seam failure causes rejection and recompositing from the original app-screen layer and official bezel; never retouch individual edges/corners or repair them generatively;
- no content or primary control is clipped, obscured, or placed in a hardware/gesture/hinge exclusion area;
- layout responds at relevant breakpoints instead of merely scaling;
- semantic colors work in light/dark mode with readable contrast;
- type hierarchy uses fp and supports system text scaling;
- the final high-fidelity renderer uses the locale-appropriate external HarmonyOS Sans file whose size and SHA-256 pass `scripts/resolve_harmonyos_fonts.py`; no synthetic style, modified font, or silent fallback is present;
- spacing and radii use defined tokens or justified exceptions;
- every frame/state records whether the status bar and navigation bar are visible, temporarily hidden, absent, or system-owned, with the correct reason;
- every normal full-screen phone app frame visibly contains exactly one HarmonyOS system navigation indicator inside the screen opening; a hardware bezel or app bottom tab/toolbar does not count as the system navigation bar;
- every present navigation indicator is a 6 vp-high capsule, is horizontally centered, has its bottom edge—not its centerline—6 vp above the bezel-derived visible screen bottom, and uses the correct responsive width for the current logical `W`;
- every present navigation state has a separate invisible `35% W x 28 vp` bottom-anchored interaction region; the visible indicator is never enlarged to that region and app-owned bottom content clears it;
- every omitted navigation bar is justified by an explicit user override, a documented absent/hidden state, or the defined single-static-game default; ambiguity outside that game-specific default is rejected and the bar is shown;
- every static phone frame shows 08:08 unless a different time is explicitly required by the scenario, and every right-side status cluster follows the supplied reference order/anatomy: NearLink, Wi-Fi 7, dual stacked 5A, and battery 100;
- every mapped-device default home screen uses the exact corresponding PNG without redesign, crop, reflow, relabeling, or content substitution; other phone models preserve the supplied icon/widget design and layout language;
- status content avoids the camera/cutout, uses black or white content appropriate to the local background, maintains at least the source-specified 1.9 contrast ratio, and remains readable across the full status region without a conflicting left/right background split;
- when the navigation bar is present, its background is visually integrated and bottom controls clear the 28 vp interaction region; scroll-end content, WebView content, scrollbars, fixed/floating actions, keyboards, half-modals, and popups remain unobscured;
- split-screen, floating-window, Picture-in-Picture, landscape, immersive, game, short-video, and compact outer-display states follow their specific status/navigation behavior instead of inheriting the normal full-screen inset;
- immersive status/navigation bars have a simple reveal gesture; navigation auto-hide timing and game anti-accidental-touch behavior are represented when relevant;
- a single static game mockup with no stated phase omits the navigation indicator and represents the post-timeout hidden phase; other single static immersive mockups default to the initial/visible phase, while a multi-frame immersive flow includes the visible and hidden phases needed to communicate the transition;
- Pura X/Pura X Max-like wide-foldable work distinguishes inner/unfolded and outer/compact display states, preserves functional continuity across folding, and applies compact outer-screen limits only in their stated scope;
- every Pura X Max unfolded landscape frame uses the official bezel rotated 90 degrees clockwise with its camera at the upper-right, while the landscape UI is freshly composited beneath the rotated alpha aperture;
- every Pura X View landscape frame uses a freshly rendered 2232 x 1320 px / 744 x 440 vp landscape screen beneath the official portrait bezel rotated exactly 90 degrees counterclockwise, with the front-facing camera on the left side; it is never a generated generic straight phone or a rotated completed portrait composite;
- every Pura X View frame uses the corrected circular camera geometry from `references/lock-screen-visual-defaults.md`; the camera center, 22 vp diameter, lens treatment, and lack of a rectangular backing remain unchanged after compositing or orientation rotation;
- every Pura X View portrait status bar anchors its left item at `x=32 vp`, its rightmost battery edge at `x=408 vp`, and both groups on the camera's `y=27 vp` optical centerline without changing the required status-cluster anatomy or scenario/default values;
- every lock-screen clock and lower shortcut pair follows `references/lock-screen-visual-defaults.md`: colors are derived from the current wallpaper rather than defaulting to unrelated opaque white, the clock uses the screenshot-reconstructed translucent treatment, and the two shortcuts preserve their circular containers, glyph anatomy, symmetric placement, bottom-system-region clearance, and localized translucent wallpaper relationship;
- every raster delivery uses lightweight-preview mode unless the user explicitly requested native/original resolution, a high-resolution final, print/final-delivery quality, or exact pixels; a preview's longest edge is at most 1600 px when a target must be chosen, all layers share one scale, and no hidden native-size output was created;
- Picture-in-Picture is reserved for supported video and video-communication continuity, uses the system control template, and follows its entry, placement, and sizing rules;
- Flash Control Ball/Window is used only for qualifying lightweight cross-app information or task interactions and follows its system-owned limits;
- every HarmonyOS security/privacy emblem uses the supplied official shield asset without altered geometry, colors, crop, or decorative effects;
- all interactive elements have clear visual feedback and adequate touch targets;
- Chinese copy obeys length, terminology, punctuation, and spacing conventions;
- accessibility, interruption, offline, failure, and recovery are addressed when relevant;
- developer handoff distinguishes fixed values, responsive rules, system-owned areas, app-owned areas, and supplied assets.
- every exact measurement and text limit is source-verified; inferred design choices are labeled as recommendations.
- every transient portrait Push Notification banner passes `references/push-notification-banner.md`: frame `(16,50,W-32,64) vp`, radius `16 vp`, responsive internal title/icon/detail/time layout, clipped color-preserving local-underlay blur, reconstructed 32% neutral-dark overlay, 24% neutral-light outline, no source-specific fixed hue, once-rounded raster conversion, and zero changes outside the rounded banner plus one-pixel antialias allowance;
- every expanded/detail lock-screen notification passes `references/lock-screen-notification-card.md`: responsive `(M,H-190,W-2M,64) vp` default frame with `M=max(16,safeInsets)`, 16 vp radius, 27 vp shortcut gap, native 32 vp logo, aligned title/detail/trailing time, clipped wallpaper-derived thin glass, reconstructed 28% tint and 40% outline defaults, no transient-banner handle, privacy-preview compliance, and zero base-lock-screen changes outside the mask plus one antialias pixel;
- every Live View deliverable names the exact form and passes the measured geometry/state/placement checks in `references/live-view-sketch-spec.md`; every expanded phone card also passes `references/expanded-live-view-card.md`, including responsive `17:6` geometry, 16 vp side margins, 50 vp top coordinate, 20 vp radius, 48% neutral dark overlay, 25% neutral outline, responsive local regions, and one-time raster rounding; reject generic pills, fixed expanded-card sizes, documentation-artboard coordinates used as device coordinates, fixed-status collisions, and duplicate capsule/card presentation.
- every WeChat conversation frame passes `references/wechat-chat-interface.md`: its `44 vp` app bar starts below the resolved HarmonyOS status region, the leading control is a standalone chevron with no `微信` label, the centered title clears the cutout/status seam, the keyboard-hidden `56 vp` composer directly adjoins the separate `28 vp` bottom interaction region without excess height, and the `32 vp` add circle ends `12 vp` from the visible right edge without an extra trailing gutter;
- every Xiaoyi phone frame passes `references/xiaoyi-assistant-interface.md`: screenshot status content is ignored; the app bar, transcript, and bottom stack are independent; `E`, `T=E+8 vp`, and `K` are resolved; every shell button uses clipped glass material; the sparkling telephone has the small star above the large star; the more control contains four small solid dots rather than large circles; all four bottom icon-label chips preserve their specified glyph anatomy; no standalone bottom-right down-arrow or scroll-to-latest control is present; user messages have a square upper-right corner and rounded other corners; the AI notice clears the indicator by `4 vp`; and the composer resolves to `(E, *, K-2E, 48) vp`;
- every expanded phone Live View frame, regardless of underlying app, scenario, phone model, or orientation, leaves the HarmonyOS status bar visible while completely covering and perceptually occluding the app-owned title region beneath it; its card/root group remains at 100% opacity, only the neutral-dark overlay layer is 48%, and broad underlay hue/luminance remains as softened local variation without recognizable back/title/tab/menu/avatar/icon/text detail. Reject uniform-gray substitution, extra gray/neutral fill, white haze, global desaturation/color replacement, decorative baked tint, or reduced parent opacity. Pura X View screenshot pixels are never reused on another model.
- every expanded phone Live View delivery includes a card-free base frame, separate-layer evidence, or an anchor comparison proving all app-owned positions and scroll state remain unchanged (`deltaX=0`, `deltaY=0`) after compositing; reject any first-content position, inset, spacer, or empty band derived from the card bottom. In established lists, feeds, and conversations, normally preceding content remains underneath the card rather than being removed or shifted below it.
- every raster expanded-card delivery runs `scripts/validate_live_view_blur_boundary.py` against the same-size card-free base and card-present frame using the once-rounded card/radius geometry; reject any changed pixel outside the rounded mask plus its one-pixel antialias allowance, any loss of sharpness outside the card, or any broad shadow/haze/glow crossing the boundary.

## Prototype and handoff requirements

For clickable prototypes and implementation packages, include:

- target devices, pixel preview frames, and vp/fp mapping;
- separate app-screen and official-bezel source layers, the bezel's native pixel dimensions, documented screen ratio, per-side alpha-derived overscan/extrusion treatment, connected-component method, and validation report with eight regional uncovered-pixel counts;
- for phone-in-hand work, the frozen validated device master, separate background/rear-hand/foreground-hand layers, explicit foreground-occlusion mask, per-boundary contact map, placement coordinates, latest edit region, contrast-background inspection evidence, and device/outside-region comparisons proving the scene did not reshape the device or regress an approved contact;
- screen/state inventory and interaction map;
- breakpoint and window-mode behavior;
- safe-area, keyboard, and hinge rules plus a per-state status/navigation bar matrix identifying system-owned versus app-owned regions, visibility, background treatment, insets, layering, reveal behavior, and acceptance evidence;
- the responsive 6 vp-high capsule navigation indicator, its 6 vp bottom-edge gap, width formula for the current logical screen width, separate `35% W x 28 vp` bottom interaction region, default 08:08 time, right-side status-cluster reference, and exact mapped home-screen asset whenever applicable;
- for every transient portrait Push Notification banner, the logical `W`, frame `(16,50,W-32,64) vp`, internal frames, vp-to-pixel mapping, final once-rounded raster edges, material stack, contrast result, and status/cutout collision verification;
- for every Xiaoyi implementation, the resolved device class, orientation, logical `W/H`, `E`, `T`, active conversation column `K`, app-bar overflow policy, transcript/result-card widths, bottom-stack/IME behavior, and explicit confirmation that the source captures' status bars were ignored;
- for every expanded phone Live View card, the logical `W`, unrounded responsive card and region calculations, vp-to-pixel mapping, final once-rounded raster frame, material/outline specification, and collision verification;
- for every expanded phone Live View overlay, a card-free base-app frame or equivalent separate-layer/anchor table proving the app bar, first content item, list/message anchors, and scroll position have zero displacement after the card is added;
- for every raster expanded phone Live View overlay, the blur-boundary validator report showing identical canvas sizes and zero changed pixels outside the rounded card plus one-pixel antialias allowance;
- component/token mapping and any justified custom component;
- the exact HarmonyOS Sans filename, resolved external path or deployment requirement, validated checksum state, variable weight, fp size, and license notice requirement;
- system-owned versus app-owned behavior;
- animation intent plus reduced-motion behavior;
- light/dark, large-text, screen-reader, muted/no-haptic, offline, error, interruption, and recovery states where relevant;
- acceptance criteria expressed as observable behavior;
- explicit notes where a demo intentionally differs from production behavior.
- the image delivery mode (`lightweight preview` or `native final`), output pixel dimensions, scale relative to the documented native canvas when applicable, and whether native-pixel validation is deferred or completed.

Never encode time-based demo transitions when the user has requested manual progression. Never silently change approved state order, screen content, or platform behavior.

## Maintenance publishing boundary

When modifying this personal skill, update the GPT Account skill only by default. Do not create or update a GitHub commit, branch, pull request, release, or repository file unless the user explicitly confirms GitHub publication for that update in the current request.

## Source handling

The user-supplied Pura X View portrait Push Notification screenshot reviewed on 2026-09-13 is a supplementary visual source only for the transient portrait-banner geometry, internal relationships, placement, and apparent wallpaper-responsive material documented in [push-notification-banner.md](references/push-notification-banner.md). The screenshot itself, its WeChat identity, private message, and red wallpaper are intentionally excluded from the skill. This supplement does not replace the core notification rules for eligibility, timing, text limits, grouping, privacy, notification-center/lock-screen presentations, immersive one-line banners, calls/alarms, badges, status icons, or Live View.

The user-supplied Pura X View lock-screen notification screenshot reviewed on 2026-09-13 is a supplementary visual source only for the expanded/detail lock-screen card geometry, title/detail/logo/time relationships, lower-shortcut anchoring, and apparent wallpaper-responsive glass documented in [lock-screen-notification-card.md](references/lock-screen-notification-card.md). The screenshot itself, its WeChat identity, sender, private message, and red wallpaper are intentionally excluded. This supplement does not replace the core notification lifecycle, grouping, privacy, user-setting, notification-center, transient-banner, call/alarm, Live View, service-card, or default collapsed lock-screen icon/count rules.

The user-supplied Pura X View Xiaoyi screenshots and magnified component crops reviewed on 2026-09-13 are supplementary visual sources only for the app-owned assistant hierarchy, component relationships, local material behavior, asymmetric user-message corners, control/icon anatomy, and responsive derivation documented in [xiaoyi-assistant-interface.md](references/xiaoyi-assistant-interface.md). The screenshots/crops, their complete status bars, annotations, conversation text, locations, weather, restaurant results, and imagery are intentionally excluded. Existing HarmonyOS status-bar rules remain authoritative on every device. Pura X View supplies the reference result only; other phones/windows must resolve their own device margin, safe areas, active column, orientation, and breakpoint behavior rather than reusing or scaling its screenshot pixels.

The 16 core bundled PDFs are the authoritative frozen baseline supplied on 2026-09-03. Later materials explicitly classified by the user as supplementary extend that baseline without replacing it; the Flash Control Ball/Window source was added on 2026-09-07, the Picture-in-Picture source on 2026-09-08, the security/privacy branding, device-bezel, Pura X Max-like wide-foldable, default-home-screen, and initial raster system-chrome supplements on 2026-09-09, the editable Live View Sketch geometry template plus corrected responsive navigation-bar screenshots on 2026-09-10, the Pura X View expanded Live View full-canvas reference plus the HarmonyOS Sans font package on 2026-09-11, the textual WeChat chat-interface reconstruction plus screenshot-derived light navigation treatment on 2026-09-12, and the device-first phone-in-hand compositing correction, detailed hand/contact/matting regression rules, Pura X View landscape mapping, textual Pura X View lock-screen reconstruction, portrait Push Notification reconstruction, plus the textual Pura X View Xiaoyi-interface reconstruction and cross-device derivation on 2026-09-13. The WeChat and Xiaoyi screenshots are intentionally excluded from the skill and repository; their status bars are out of scope, and their personal content must never be reproduced. The iterative phone-in-hand correction screenshots and the Pura X View lock-screen screenshot are likewise excluded: retain only their generalized or measured textual rules, not the source images or private content. The font package is authoritative for binary filenames, internal family names, variable-weight ranges, glyph files, hashes, and license; keep those binaries external to this skill and resolve them through [harmonyos-sans-font-assets.md](references/harmonyos-sans-font-assets.md). The corrected navigation screenshots supersede only the earlier fixed-pixel navigation-indicator geometry and placement. The Pura X View lock-screen supplement controls its camera shape, size, placement, portrait status-edge anchors, and the cross-device lock-screen clock/shortcut material relationship; it does not replace the right-side status-cluster anatomy, default status values, navigation geometry, or other device bezels. The Pura X View full-canvas reference supersedes conflicting expanded **phone-card** geometry, placement, corner radius, material, and internal-region values from the older Sketch template; it does not change the base/collapsed card, status-bar capsules, compact circular outer-display treatment, Live View behavior, navigation bar, or device bezel. The Xiaoyi supplement controls only the app-owned assistant interface; existing HarmonyOS system chrome remains authoritative. The PDF text files are search-friendly derivatives. A compact installation may store the byte-exact PDFs as multipart `references/source-pdfs-archive/frozen-source-pdfs.tar.xz.part-*`; use `scripts/extract_frozen_pdf.py` to recover an authoritative PDF without altering it. For Live View, keep the current guide authoritative for eligibility, lifecycle, text limits, privacy, colors, and interaction; use `references/live-view-sketch-spec.md` for unaffected forms and `references/expanded-live-view-card.md` for expanded phone cards. For the security/privacy shield, the supplied `.ai` artwork is authoritative over its SVG and PNG convenience exports. For device presentation, the supplied bezel mapping and assets control over generic phone-frame conventions. The mapped home-screen PNGs—or PNGs materialized with verified identical RGB pixels from their lossless WebP storage masters—control over generic launcher layouts for their named devices, and the supplied status crop controls the default right-side status-cluster anatomy/order. If a supplementary source appears to conflict with the core baseline, preserve the core rule, flag the difference, and ask for direction unless the user has defined a narrower precedence. When future official documentation may have changed, do not silently replace any supplied source. If the user explicitly asks for current official guidance, verify against Huawei's official developer documentation, report material differences, and update this skill only when requested.
